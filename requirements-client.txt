# Minimal packages for MetroidBreadClient + ap_core CommonClient / Utils
# when Hub spawns system Python (SKIP_REQUIREMENTS_UPDATE skips full AP requirements).
# Also installs open-dread-rando so Patch can use the same interpreter Hub/Connect ensures.
websockets>=13.0.1,<14
colorama>=0.4.6
PyYAML>=6.0.3
pathspec>=0.12.1
certifi>=2025.11.12
platformdirs>=4.5.0
jellyfish>=1.2.1
typing_extensions>=4.15.0
schema>=0.7.8
# CommonClient → ModuleUpdate needs pkg_resources; without it Hub hangs on "press enter".
setuptools>=75,<81
# Patcher engine (Randovania ODR). >=2.19 for DNA HUD / Samus-menu upgrade rows.
open-dread-rando>=2.19
